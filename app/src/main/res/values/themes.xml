<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Thème principal : nuit nordique, barres système transparentes (edge-to-edge) -->
    <style name="Theme.MKCopilot" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/brand_night</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Splash : l'emblème bouclier-fjord sur charbon nuit, fondu vers l'accueil -->
    <style name="Theme.MKCopilot.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="windowSplashScreenAnimationDuration">450</item>
        <item name="postSplashScreenTheme">@style/Theme.MKCopilot</item>
    </style>

</resources>
